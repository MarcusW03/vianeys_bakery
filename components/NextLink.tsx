'use client';
// Next.js v16 requires a 'use client' wrapper when passing next/link to MUI's
// `component` prop. Without this, Next.js throws a serialization error.
// Usage: <Button component={NextLink} href="/path">
export { default } from 'next/link';
